(use-package elcord)

(require 'elcord)

;; 设置你的 Discord 应用程序客户端 ID
;;(setq discord-client-id "YOUR_DISCORD_CLIENT_ID")

;; 启动时自动连接
;;(add-hook 'after-init-hook #'discord-mode)

;; 自定义状态更新函数（可选）
;;(defun my-discord-update-status ()
;;  (discord-rpc-set-state "Editing code")
;;  (discord-rpc-set-details "In Emacs"))

;;(add-hook 'emacs-startup-hook #'my-discord-update-status)
